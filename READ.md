# Getting Started

## How to start
First thing: run the docker containers.

run the following command in the root directory of the project:
```
docker compose -v up -d
```


to stop the docker containers do
```
docker compose down
```